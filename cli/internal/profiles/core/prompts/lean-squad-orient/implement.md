Write the Lean Squad orient artefacts using the analysis from the previous phase.

Vessel: {{.Vessel.ID}}
Ref: {{.Vessel.Ref}}

## Analysis from the previous phase

{{.PreviousOutputs.analyze}}

## Your task

Create or update two files:

### 1. `formal-verification/RESEARCH.md`

Free-form approach notes. Must start with the following line (exactly) on its own:

```
🔬 Lean Squad artefact — generated by the `lean-squad-orient` workflow. Treat as a living document; correct anything wrong.
```

Then include sections:

- `## Repo overview (FV angle)` — 1-3 paragraphs summarising what this codebase does,
  focused on what is relevant for formal verification (languages, purity, side-effect
  boundaries, key domains).
- `## Candidate target families` — bullet list of the broad categories of FV-tractable
  code you found (parsers, state machines, crypto, etc.) with pointers to the directories
  where they live.
- `## Toolchain notes` — any caveats about language interop, FFI, or missing
  infrastructure that will affect later phases. If everything looks fine, say so.

If `formal-verification/RESEARCH.md` already exists, preserve prior content below a new
`## Update {{.Vessel.ID}}` section rather than overwriting — each orient run appends a
fresh perspective.

### 2. `formal-verification/TARGETS.md`

A prioritised table of candidate targets. Must start with the same 🔬 disclosure line
shown above, then a short paragraph explaining what the table is, then a Markdown table
with these columns **in this order**:

| Name | Source path | Why tractable | Effort | Status |

- **Name:** short slug (e.g. `queue-dedup`).
- **Source path:** file path and function name where applicable
  (e.g. `cli/internal/queue/queue.go:dedupe`).
- **Why tractable:** 1-2 sentences.
- **Effort:** `S`, `M`, or `L`.
- **Status:** `[ ] pending` for new rows.

Rules:

- Add rows for each candidate from the analysis (aim for 3-5 in total across the file,
  including any prior pending rows).
- If the file already exists, preserve existing rows (especially any marked `[x]`) and
  append the new candidates below them.
- Do not modify rows marked `[x]` done. Do not mark any new row as done.

## Important constraints

- This workflow is the **sole writer** of `formal-verification/RESEARCH.md` and
  `formal-verification/TARGETS.md`. Do not touch any other file under
  `formal-verification/` or anywhere else in the repo.
- Do not run `lake build` or any Lean toolchain commands — that's the bootstrap workflow's
  job. This phase is pure documentation.
- If `formal-verification/` does not yet exist as a directory, create it along with the
  two files. The bootstrap workflow will fill out the rest of the scaffolding later.

Write the files now.
