Open a pull request with the orient artefacts.

Vessel: {{.Vessel.ID}}
Ref: {{.Vessel.Ref}}

## Analysis
{{.PreviousOutputs.analyze}}

## Implementation summary
{{.PreviousOutputs.implement}}

## Your task

1. Stage only the orient artefacts: `git add formal-verification/RESEARCH.md formal-verification/TARGETS.md`. Do not add any other files.
2. Commit with a clear message, e.g. `docs(lean-squad): orient — propose FV targets`.
3. Push the current branch.
4. Open a PR on top of `{{.Repo.DefaultBranch}}`:

```
gh pr create \
  --title "[Lean Squad] Orient: identify FV targets" \
  --body "<body>"
```

The body must:

- Start with the 🔬 disclosure: "Generated by the `lean-squad-orient` workflow as part of Lean Squad. Doctrine and task design follow agentics' lean-squad (https://github.com/githubnext/agentics/blob/main/docs/lean-squad.md)."
- Briefly list each proposed target (name + one-line "why tractable").
- Note that this PR is documentation-only; no code or Lean toolchain changes.
- Include the structured marker on its own line so the tick coordinator can reconcile state:

```
LEAN_SQUAD_RUN: {"task":"orient","target":"repo","status":"ok","artefact":"formal-verification/TARGETS.md"}
```

Do not add issue-linking footers; this vessel is enqueued, not issue-driven. Do not apply
`ready-to-merge` yet — the tick coordinator handles merge hygiene for `[Lean Squad]` PRs.
