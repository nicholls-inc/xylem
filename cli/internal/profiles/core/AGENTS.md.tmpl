# Agents guide

> Generated by `xylem init --profile=core` for **{{.Repo}}**.
> Keep this file at the repo root — every agent session reads it at startup.

## What agents do in this repo

<!-- TODO: Describe what this repository does in one or two sentences. -->
<!-- Example: "Go CLI that schedules autonomous Claude Code sessions." -->

## Build & test commands

<!-- TODO: Replace with your project's actual commands. -->

```bash
# build:   <your build command>
# test:    <your test command>
# lint:    <your lint command>
# format:  <your format command>
```

## Rules agents must follow

1. Run lint and tests before opening a pull request.
2. Never commit secrets, credentials, or `.env` files.
3. Keep changes scoped — one concern per pull request.
4. Do not modify files in `.xylem/phases/` — these are runtime outputs.

## Workflow labels

Issues labelled with these trigger xylem workflows automatically:

| GitHub label | Workflow |
|---|---|
| `xylem:fix-bug` | Analyse → implement → verify → PR |
| `xylem:implement-feature` | Plan → implement → verify → PR |
| `lean-squad-opt-in` | Opt in to the Lean Squad 8h tick (Lean 4 formal verification); see [docs](https://github.com/nicholls-inc/xylem/blob/main/docs/workflows/lean-squad.md) |
| `lean-squad-focus` | Run a specific lean-squad task now (details in issue body) |

## Further reading

- Workflow YAML: `.xylem/workflows/`
- Prompt templates: `.xylem/prompts/`
- Runtime config: `.xylem.yml`
- Harness instructions: `.xylem/HARNESS.md`

Full documentation: https://github.com/nicholls-inc/xylem
